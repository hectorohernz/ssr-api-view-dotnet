@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="hero">
  <h2>Insporation Portal.</h2>
</div>

@*<div class="card-columns">
    @foreach (var user in Model.Users)
    {
    <div class="card">
        <div class="card-img" style="background-image: url('@user.userProfileImage')"></div>
        <div class="card-body">
            <h5 class="card-title">@user.fullName</h5>
        </div>
    </div>
    }
</div>*@
